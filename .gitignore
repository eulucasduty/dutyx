# Lixo de sistema
.DS_Store
Thumbs.db

# Node (se algum script opcional usar)
node_modules/

# Sessões do Claude Code (estado local, não versionar)
.claude/sessions/
.claude/.cache/

# ⚠️ De propósito, saidas/ e o seu logo VÃO pro backup:
# seu repositório é PRIVADO (o /salvar cria assim) e o backup existe
# justamente pra guardar o SEU trabalho. Se um dia você tornar o repo
# público, tire saidas/ e o logo antes de publicar.

# 🔒 SEGURANÇA — NUNCA versionar chaves/segredos
.mcp.json
.env
.env.*
*.key
*.pem
*.token
**/secrets*
.claude/settings.local.json

# Mídia pesada (não vai pro backup — o GitHub trava arquivo acima de 100 MB)
*.mp4
*.mov
*.mkv
*.avi
*.webm
*.wav
*.mp3
*.m4a
setup/whisper/
